# Offline sample workspace

Open `sample-review.prw`, then run **Protheus Agent: Review Active ADVPL/TLPP File**. The intentional inline conditional produces deterministic finding `CA4000` and demonstrates the Problems/evidence workflow without Protheus, Docker, a database, Hermes or an AI account.

This sample was authored for Protheus Engineering Agent and is distributed under the repository's Apache-2.0 license. It contains no TOTVS/customer artifact. The `User Function` form and three-argument `Iif` usage were checked against the installed `advpl-exemplos-validados` corpus; no corpus source was copied.
