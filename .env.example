# Copy values into your process environment; never commit real credentials.
PEA_ENVIRONMENT=development
PEA_WORKSPACE=/path/to/protheus-workspace
# PEA_HERMES_COMMAND=hermes
# PEA_GRANTS=context:write
# PEA_TDN_SNAPSHOT=/path/to/versioned-tdn-snapshot.json
# PEA_DICTIONARY_SNAPSHOT=/path/to/versioned-dictionary-snapshot.json
# Oracle drivers, AI providers, compiler credentials and approval handlers are injected by the host.
# They are intentionally not configured through this public template.
